# AI Optimization Report
Overall accuracy: 92.9% (75/120 passed)

## Suggested Prompt Edits

### Edit 1: Clarify Hallucination Risks for Deadlines
File: src-tauri/src/commands/ai.rs
Function: analyze_email_claude
Dimension(s): no_false_deadline (89.3%), life_data_confirmation (0%)
Reasoning: The model is hallucinating deadlines (e.g., "2026-02-27") in generic emails like logo feedback or team threads where no date was specified. This change explicitly instructs the model to avoid fabricating dates when they aren't in the text.

Old:
- Deadline: Extract a deadline ONLY if the user personally must complete an action BY that date or face a consequence (missing out, late penalty, expiration). Use ISO format (YYYY-MM-DD). Set to null if no deadline applies.

New:
- Deadline: Extract a deadline ONLY if the user personally must complete an action BY that date or face a consequence (missing out, late penalty, expiration). Use ISO format (YYYY-MM-DD). Set to null if no deadline applies.
CRITICAL: Do NOT generate or hallucinate a deadline if a specific date is not explicitly mentioned in the email text. If the email implies urgency (e.g., "soon", "ASAP") but gives no date, you may use today's date for "ASAP" but must set to null for vague future urgency.

### Edit 2: Refine "Reply Required" for Mass Marketing & Events
File: src-tauri/src/commands/ai.rs
Function: analyze_email_claude
Dimension(s): requires_reply_class (87.3%)
Reasoning: The model currently flags marketing emails (Conferences, Marathons) as requiring a reply because they contain questions like "Register now?". This edit clarifies that calls-to-action in mass marketing do not constitute a personal requirement to reply.

Old:
IMPORTANT - Set requires_reply to FALSE for:
- Newsletters, marketing emails, notifications, or automated emails

New:
IMPORTANT - Set requires_reply to FALSE for:
- Newsletters, marketing emails, notifications, or automated emails
- Mass marketing or promotional emails (conference early birds, sale announcements, webinars) even if they contain rhetorical calls-to-action like "Register now" or "Buy tickets". These are not personal requests.

### Edit 3: Fix Life Data Extraction for E-commerce
File: src-tauri/src/commands/ai.rs
Function: analyze_email_claude
Dimension(s): life_data (73.7%), life_data_confirmation (0%)
Reasoning: Model is extracting "package" for Amazon orders but failing to extract "order" or "confirmation code" fields, and missing "purchase" in digital receipts. This update explicitly adds these sub-types to the extraction guidelines.

Old:
  * "package" — shipping/delivery notifications. Include carrier (UPS, FedEx, USPS, Amazon), tracking_number, date (delivery date), details (item description).

New:
  * "package" — shipping/delivery notifications. Include carrier (UPS, FedEx, USPS, Amazon), tracking_number, date (delivery date), details (item description).
  * "order" — e-commerce order confirmations (Amazon, etc.). Include confirmation_code, order_number, details.
  * "purchase" — digital purchases or receipts (App Store, etc.). Include amount, currency, date, details (item name).

### Edit 4: Tone Classification Adjustment
File: src-tauri/src/commands/ai.rs
Function: classify_email
Dimension(s): deadline_display (66.7% - impacted by incorrect tone/context), crm_sender_parse (50%)
Reasoning: "Urgent" is over-classified for automated reminders (Doctor, Subscription) and tone is misclassified as "demanding" for professional requests. Adjusting the strictness of "urgent" helps prevent these false positives.

Old:
"urgent" — ONLY use when ALL of these are true:
  - Immediate time pressure (within 24-48 hours: "ASAP", "immediately", "today", "tomorrow", due date is tomorrow or today)
  - Requires the user's personal action (not just FYI about something time-sensitive)
  - From a real person or organization that matters (not automated/marketing)

New:
"urgent" — ONLY use when ALL of these are true:
  - Immediate time pressure (within 24-48 hours: "ASAP", "immediately", "today", "tomorrow", due date is tomorrow or today)
  - Requires the user's personal action (not just FYI about something time-sensitive)
  - From a real person or organization that matters (not automated/marketing)
  NOT urgent: Automated reminders (doctor appointments, subscription renewals) or standard professional requests.

## Code Issues (Non-Prompt)
- File: src-tauri/src/commands/ai.rs — In function `analyze_email_claude`, the `meeting_request` object is populated even when `is_meeting` is false (e.g., B3, I1). While the prompt sets `is_meeting: false`, the model fills `event_type`, `duration`, etc., with defaults (60 mins). The parsing code should explicitly ignore/nil out meeting details if `is_meeting` is false to prevent confusion.
- File: src-tauri/src/commands/ai.rs — `crm_sender_parse` (O3) is failing because the code likely expects a specific format for "noreply" detection. The raw output shows `noreply=false` for `alice.wong@team.company.com`. If this is considered a parseable sender, the validation logic might be too strict or the prompt needs to explicitly define "parseable sender" vs "name". (Prompt fix suggested in Edit 1 covers the definition, code may need to align).