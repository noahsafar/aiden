# AI Optimization Report
Overall accuracy: 95.1% (90/120 passed)

## Suggested Prompt Edits

### Edit 1: Fix "Conference/Event" vs "Meeting" distinction
File: src-tauri/src/commands/ai.rs
Function: analyze_email_claude
Dimension(s): meeting_detection
Reasoning: The model currently treats broad "invitations" to events (conferences, webinars) as meeting requests. We need to explicitly exclude mass-marketing/open-invite events where the user is not being scheduled into a specific slot.

Old:
- Meeting/Event: Set is_meeting=true if there's a meeting OR an event the user could attend.
  * event_type: "meeting" if someone wants to schedule a 1-on-1 or group meeting. "event" if it's a talk, seminar, workshop, webinar, lecture, presentation, panel, networking event, office hours, or any scheduled event the user is invited to attend.

New:
- Meeting/Event: Set is_meeting=true ONLY if the sender is requesting a specific time slot with the user (scheduling a meeting, 1-on-1, interview, or specific RSVP to a distinct event).
  * event_type: "meeting" if someone wants to schedule a 1-on-1 or group meeting. "event" if the user is specifically invited to a distinct scheduled occurrence (talk, seminar, workshop, webinar, lecture, presentation, panel, networking event, office hours, doctor appointment, court date).
  * EXCLUSION: Do NOT set is_meeting=true for mass-marketing conference announcements, webinar promos, or generic "invitations" to large events where no specific RSVP/interaction is requested (e.g. "DevCon is open for registration").

### Edit 2: Fix Tone Hallucination ("firm" vs "authoritative")
File: src-tauri/src/commands/ai.rs
Function: analyze_email_claude
Dimension(s): tone
Reasoning: The test expects "authoritative" for professor deadline extensions, but the model returns "firm". Restricting the vocabulary forces the model to pick from valid options, preventing "creative" tone labels that fail validation.

Old:
- Sender tone: Detect the emotional tone of the email. Use one word like "friendly", "neutral", "frustrated", "angry", "anxious", "excited", "apologetic", "demanding", "grateful", "formal", "casual". This will be used to adapt the reply tone.

New:
- Sender tone: Detect the emotional tone of the email. Choose EXACTLY ONE of these words: "friendly", "neutral", "professional", "formal", "casual", "urgent", "direct", "authoritative", "apologetic", "excited", "grateful", "anxious", "frustrated", "angry", "demanding". This will be used to adapt the reply tone.

### Edit 3: Improve Deadline Extraction
File: src-tauri/src/commands/ai.rs
Function: analyze_email_claude
Dimension(s): deadline_detection
Reasoning: The model is missing deadlines in invoices (NET 30) and warranty expiration notices because the prompt focuses heavily on "action by" language. We need to explicitly instruct it to look for due dates associated with financial documents and expirations.

Old:
- Deadline: Extract a deadline ONLY if the user personally must complete an action BY that date or face a consequence (missing out, late penalty, expiration). Use ISO format (YYYY-MM-DD). Set to null if no deadline applies.

New:
- Deadline: Extract a deadline if the user personally must complete an action BY that date or if a financial/subscription term expires.
  * ACTIONS: "by", "before", "due", "closes on", "respond by", "RSVP by".
  * FINANCIAL/EXPIRATIONS: Payment due dates (NET 30, Due Date), warranty expiration dates, contract renewal deadlines, subscription end dates.
  * ISO Format: YYYY-MM-DD.

### Edit 4: Better Life Data Extraction
File: src-tauri/src/commands/ai.rs
Function: analyze_email_claude
Dimension(s): life_data_confirmation
Reasoning: The model fails to extract confirmation codes (e.g., "XYZ789") from flight confirmations into `life_data`. Adding an explicit instruction to look for confirmation codes/order numbers in the `life_data` block ensures this data is captured.

Old:
  * "travel" — flights, hotel bookings, trip confirmations. Include carrier (airline/hotel), date (departure), end_date (return), details (confirmation #).

New:
  * "travel" — flights, hotel bookings, trip confirmations. Include carrier (airline/hotel), date (departure), end_date (return), details (confirmation code, record locator, e-ticket number).
  * "confirmation_code" — Generic extraction for any alphanumeric confirmation code (e.g., "XYZ789", "AB-123").

### Edit 5: Fix "Requires Reply" for Specific Categories
File: src-tauri/src/commands/ai.rs
Function: analyze_email_claude
Dimension(s): requires_reply_class
Reasoning: The model is setting `requires_reply` to false for automated financial documents (invoices) and recurring sender updates that imply a future action (1:1 discussion). We need to explicitly treat financial approval requests and "saved for later" topics as not requiring an immediate reply.

Old:
IMPORTANT - Set requires_reply to FALSE for:
- Newsletters, marketing emails, notifications, or automated emails
- Mass marketing or promotional emails (conference early birds, sale announcements, webinars) even if they contain rhetorical calls-to-action like "Register now" or "Buy tickets". These are not personal requests.

New:
IMPORTANT - Set requires_reply to FALSE for:
- Newsletters, marketing emails, notifications, or automated emails
- Mass marketing or promotional emails (conference early birds, sale announcements, webinars) even if they contain rhetorical calls-to-action like "Register now" or "Buy tickets". These are not personal requests.
- Automated invoices, bills, or financial statements where no input is required (e.g., "Here is your invoice").
- Project status updates or notes where the email explicitly states the topic is "saved for a future meeting" (e.g., "Let's discuss this in our 1:1").

### Edit 6: Prevent False Deadlines in Reminders
File: src-tauri/src/commands/ai.rs
Function: analyze_email_claude
Dimension(s): no_false_deadline
Reasoning: The model extracts the *event date* (e.g., prescription pickup, appointment date) as the *deadline*. The deadline field should only be populated if there is an action *due by* a specific date, distinct from the event itself.

Old:
  CRITICAL HALLUCINATION CHECK: Do NOT set a deadline unless a specific date or day is written in the email text. "ASAP", "urgent", "soon", "immediately", or "right now" WITHOUT a date → deadline is null. Only "by end of day", "by today", "by Friday", "due March 5" etc. count as real deadlines.

New:
  CRITICAL HALLUCINATION CHECK: Do NOT set a deadline unless a specific date or day is written in the email text.
  * DEADLINE NULL: If the email is purely a reminder of an event (e.g., "Appointment on Feb 25", "Prescription ready for pickup"), the deadline is NULL unless there is a separate "action required by" date.
  * VALID DEADLINE: "by end of day", "by today", "by Friday", "due March 5", "Submit by X", "Payment due by X".

### Edit 7: Fix CRM Sender Parsing
File: src-tauri/src/commands/ai.rs
Function: classify_email
Dimension(s): crm_sender_parse
Reasoning: The user prompt template does not explicitly ask the `classify_email` prompt to output the sender's name/email. The prompt focuses on content analysis, so the model might hallucinate `null` for the name if it's not in the `From` field provided or if it's not explicitly instructed to normalize it.

Old:
Classify this email's priority. Be CONSERVATIVE — most emails are "normal". Only escalate when truly warranted.

Today's date: {}
From: {}
Subject: {}
Content: {}

Respond in JSON format:
{{
  "category": "urgent|important|normal|low",
  "confidence": 0.95,
  "requires_reply": true,
  "can_auto_archive": false
}}

New:
Classify this email's priority. Be CONSERVATIVE — most emails are "normal". Only escalate when truly warranted.

Today's date: {}
From: {}
Subject: {}
Content: {}

Respond in JSON format:
{{
  "category": "urgent|important|normal|low",
  "confidence": 0.95,
  "requires_reply": true,
  "can_auto_archive": false,
  "sender_name": "Extract the sender's full name from the From field. If format is 'First Last <email>', return 'First Last'. If 'noreply' or 'no-reply', return 'noreply'.",
  "sender_email": "Extract the email address from the From field.",
  "is_noreply": "true if the sender is noreply, no-reply, or auto-generated; false otherwise."
}}

## Code Issues (Non-Prompt)
- File: src-tauri/src/commands/ai.rs — The `life_data` JSON schema in the output format does not explicitly list a top-level `confirmation_code` type (it relies on `travel` or `order`). The test failure for P2 (`life_data_confirmation`) suggests the model might be placing the code `XYZ789` inside `travel` (which matches the prompt instruction) but the test code might be looking for it in a flattened field or a different structure. Verify that the JSON schema matches the prompt's implied structure (e.g., `travel.details`). If the test expects a top-level field, the prompt structure needs to allow for it.