# AI Optimization Report
Overall accuracy: 95.1% (90/120 passed)

## Suggested Prompt Edits

### Edit 1: Clarify Action Deadlines vs. Event Dates
File: src-tauri/src/commands/ai.rs
Function: analyze_email_claude
Dimension(s): deadline_detection, requires_reply_class
Reasoning: The model currently hallucinates deadlines for event start dates (e.g., "Conference is March 15") and auto-renewal dates where no user action is explicitly requested (e.g., "We will auto-renew on March 3"). This edit distinguishes between an event occurring on a date and an action required by a date, and specifically targets the "event_on" vs "register_by" confusion seen in tests L3, B3, E2, S2, and Y4.

Old:
- Deadline: Extract a deadline ONLY if the user personally must complete an action BY that date or face a consequence (missing out, late penalty, expiration). This includes payment due dates, warranty expirations, contract renewal deadlines, and registration closing dates. Use ISO format (YYYY-MM-DD). Set to null if no deadline applies.

New:
- Deadline: Extract a deadline ONLY if the user personally must complete an action BY that date. This includes RSVP dates, registration cutoffs, payment due dates, and "respond by" dates.
* Set to null for event start times (e.g., "Conference is March 15") or informational dates (e.g., "Auto-renewal effective March 3") unless the text explicitly demands action by that date (e.g., "Cancel by March 3").
* Use ISO format (YYYY-MM-DD). Set to null if no deadline applies.

### Edit 2: Fix Meeting Detection for "Soft" Invites and Scheduling
File: src-tauri/src/commands/ai.rs
Function: analyze_email_claude
Dimension(s): meeting_detection
Reasoning: The system fails to detect meeting requests in urgency contexts (L1), confuses "register by" dates for the event itself (L3), hallucinates meetings for past references (M1), and incorrectly flags vague "lets catch up" emails (V4). This edit sharpens the distinction between a confirmed meeting request, a proposed scheduling poll, and a mere topic suggestion.

Old:
- Meeting/Event: Set is_meeting=true ONLY if the user is specifically invited to a distinct scheduled occurrence or someone is requesting a specific time slot with the user.
  * event_type: "meeting" if someone wants to schedule a 1-on-1 or group meeting, or a personal appointment (doctor, dentist, etc.). "event" if it's a talk, seminar, workshop, webinar, lecture, presentation, panel, networking event, office hours, or any distinct scheduled event the user is invited to attend.

New:
- Meeting/Event: Set is_meeting=true ONLY if the user is invited to a specific occurrence or a specific time slot is proposed/requested.
  * event_type: "meeting" (1-on-1 or group) or "event" (conference, webinar, etc.).
  * EXCLUSIONS: 
    - Set is_meeting=false for vague social emails like "Let's catch up" without proposed times.
    - Set is_meeting=false for "scheduling polls" (e.g., Doodle links, "Which time works?") unless a specific slot is already offered in the text.
    - Set is_meeting=false for past event references (e.g., "Did I miss anything from the meeting?").
    - Set is_meeting=false for marketing emails mentioning an event date unless explicitly inviting the user to that specific instance.

### Edit 3: Strict Life Data Classification
File: src-tauri/src/commands/ai.rs
Function: analyze_email_claude
Dimension(s): life_data
Reasoning: Tests R1 and R5 show incorrect `life_data` tagging where "order" is missing or "purchase" is hallucinated. Test X4 shows a "subscription" misclassified as "deadline". This edit enforces strict keyword matching to prevent hallucination of categories not supported by the text.

Old:
- Life data: Extract structured life intelligence items from the email. Each item has a data_type and relevant fields:
  * "subscription" — recurring services (Netflix, Spotify, gym memberships, SaaS). Include amount, currency, frequency, renewal date.
  * "bill" — one-time or recurring bills/invoices (utilities, rent, insurance). Include amount, currency, date (due date), frequency.
  * "travel" — flights, hotel bookings, trip confirmations. Include carrier (airline/hotel), date (departure), end_date (return), confirmation_number (record locator, booking ref, e-ticket number e.g. "XYZ789"), details.
  * "package" — shipping/delivery notifications. Include carrier (UPS, FedEx, USPS, Amazon), tracking_number, date (delivery date), details (item description).
  * "order" — e-commerce order confirmations (Amazon, etc.). Include confirmation_code, order_number, details.
  * "purchase" — digital purchases or receipts (App Store, etc.). Include amount, currency, date, details (item name).
  * "insurance" — insurance claims, policy changes, coverage notices. Include amount, currency, date, details (claim #, policy info).
  * "financial" — tax refunds, deposits, bank notifications, investment updates. Include amount, currency, date, details.
  * "deadline" — any actionable cutoff date requiring the user's action (application deadlines, payment due dates, renewal deadlines, RSVP-by dates, respond-by dates). NOT event/meeting times. Always include here even if also set in the top-level deadline field. Include date, details.

New:
- Life data: Extract structured life intelligence items. ONLY use the specific types below:
  * "order" — Order confirmations (Amazon, etc.). Keywords: "order confirmation", "order #".
  * "package" — Shipping notifications. Keywords: "shipped", "tracking", "out for delivery", "estimated delivery".
  * "purchase" — Digital receipts. Keywords: "receipt", "invoice", "purchase of".
  * "subscription" — Recurring services. Keywords: "subscription", "membership", "renewing". NOT warranties.
  * "bill" — Utility/rent bills. Keywords: "bill", "statement", "amount due".
  * "travel" — Flights/hotels. Keywords: "itinerary", "booking", "confirmation", "e-ticket".
  * "insurance" — Policy/Claims. Keywords: "policy", "claim #".
  * "financial" — Bank/Refunds. Keywords: "deposit", "refund", "payment received".
  * "deadline" — Valid deadlines (as defined in the Deadline section). DO NOT use this type for warranty expirations or event start dates.
  IMPORTANT: Do not hallucinate types. If the email mentions a warranty expiration (X4), do not tag it as life_data unless it explicitly mentions "subscription" or "membership renewal".

### Edit 4: Hallucination Check for Deadlines and Questions
File: src-tauri/src/commands/ai.rs
Function: analyze_email_claude
Dimension(s): crm_sender_parse, deadline_detection, questions
Reasoning: The model is hallucinating dates (M1, P4, E2) and questions (L2, M5, N3) where none exist or are irrelevant. This adds a negative constraint to explicitly prevent these common errors.

Old:
- Questions: Extract questions, decisions needed, AND implicit requests for input/deliverables from the email. Include:
  1. Direct questions ("What is the status?")
  2. Requests for documents/files ("Send me the report", "Can you share the file?")
  3. Requests for status/updates ("Need an update on X")
  4. Decision prompts ("Can you attend?" → choice with Yes/No)

New:
- Questions: Extract questions and decisions needed.
  * Include: Direct questions ("What is the status?"), requests for documents ("Send me the report"), status updates ("Need an update on X"), and decision prompts ("Can you attend?").
  * IMPORTANT: If the email body is empty or purely informational, set questions to [].

### Edit 5: Handling "Overdue" and "Hallucinated" Dates
File: src-tauri/src/commands/ai.rs
Function: analyze_email_claude
Dimension(s): requires_reply_class, deadline_detection
Reasoning: The model triggers a "requires_reply" and a generic "ASAP" question for overdue tasks (N3) and defaults to the current year for dates in templates (M1, E2, P4). This edit suppresses replies for overdue action items and prevents date hallucination from context.

Old:
- Deadline: Extract a deadline ONLY if the user personally must complete an action BY that date or face a consequence (missing out, late penalty, expiration). This includes payment due dates, warranty expirations, contract renewal deadlines, and registration closing dates. Use ISO format (YYYY-MM-DD). Set to null if no deadline applies.
  CRITICAL HALLUCINATION CHECK: Do NOT set a deadline unless a specific date or day is written in the email text AND it represents an action cutoff.

New:
- Deadline: Extract a deadline ONLY if the user personally must complete an action BY that date.
  * HALLUCINATION CHECK: Do NOT set a deadline unless a specific date/day is written in the text.
  * EXCEPTION: If a deadline mentions a date in the past (e.g., "You missed yesterday's deadline"), treat it as informational and set requires_reply to FALSE unless the sender explicitly asks for a status update on the overdue item.
  * Do NOT infer the year from "Today's date" for relative dates like "March 3rd" if the context implies a template or automated email; default to the explicit year in text or null if ambiguous.

## Code Issues (Non-Prompt)
- File: src-tauri/src/commands/ai.rs — The CRM sender parse failure (O3: `alice.wong@team.company.com`) where `name=null` suggests the code logic for `extract_sender_name` is failing on hyphenated emails or internal subdomains. A regex update or fallback to "Alice" (from local-part) is recommended, though this is borderline correct behavior (no name present in header).